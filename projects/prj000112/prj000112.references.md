# References: prj000112 - Pre-Commit Ruff Integration

## Existing Code References

### Primary Integration Points

#### .pre-commit-config.yaml
**Location**: `/home/dev/PyAgent/.pre-commit-config.yaml`
**Purpose**: Pre-commit hook configuration
**Key Content**:
```yaml
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: <version>
  hooks:
    - id: ruff
      args: [--fix]
    - id: ruff-format
```

**Integration Plan**:
1. Use RuffVersionManager to validate hook version
2. Ensure version matches pyproject.toml
3. Run version check before hook execution
4. Provide version upgrade guidance if needed

**Usage**:
```python
from ruff_precommit import RuffVersionManager

mgr = RuffVersionManager(min_version="0.5.0")
compatible, msg = mgr.check_version_compatibility()
if not compatible:
    print(f"Update Ruff: {msg}")
```

#### pyproject.toml
**Location**: `/home/dev/PyAgent/pyproject.toml`
**Purpose**: Project configuration including tool.ruff
**Key Section**:
```toml
[tool.ruff]
line-length = 100
select = ["E", "F", "W"]
ignore = ["E203", "W503"]
```

**Integration Points**:
1. RuffConfig reads from pyproject.toml
2. Version requirement should be specified
3. CI should enforce consistency

**Reference**:
```python
from ruff_precommit import RuffConfig

config = RuffConfig("pyproject.toml")
is_valid, errors = config.validate()
```

### Secondary References

#### .flake8
**Location**: `/home/dev/PyAgent/.flake8`
**Relevance**: Legacy linting configuration
**Relationship**: Ruff is modernized replacement
**Migration**: Ruff config mirrors .flake8 rules

#### .pylintrc
**Location**: `/home/dev/PyAgent/.pylintrc`
**Relevance**: Alternative linting configuration
**Relationship**: Ruff complements with faster checks

### CI/CD Infrastructure References

#### GitHub Actions Workflows
**Location**: `/home/dev/PyAgent/.github/workflows/`
**Purpose**: Define build and test pipelines
**Integration Points**:
1. Add Ruff version check step
2. Compare CI version with pre-commit
3. Fail if mismatch detected

**Future Enhancement**:
```yaml
- name: Check Ruff Version Consistency
  run: |
    cd projects/prj000112
    python -c "from ruff_precommit import RuffVersionManager; \
               m = RuffVersionManager('0.5.0'); \
               ok, msg = m.check_version_compatibility(); \
               exit(0 if ok else 1)"
```

### Python Environment References

#### requirements.txt
**Location**: `/home/dev/PyAgent/requirements.txt`
**Current**: Ruff version pinning
**Integration**:
- Pin specific Ruff version
- RuffVersionManager can validate
- Ensure consistency across environments

#### Ruff Installation
**Standard Method**:
```bash
pip install ruff==0.5.0
# OR
cargo install ruff  # Rust-based installation
```

**Version Detection**:
```bash
ruff --version
# Output: ruff 0.5.0
```

## Code Patterns Used in PyAgent

### 1. Subprocess Execution Pattern
**Example**: Used in RuffVersionManager.get_installed_version()
**Origin**: Python subprocess module
**Consistency**: Matches patterns in src/tools/

**Related Code**:
- `src/tools/git_utils.py`: Git subprocess calls
- `src/tools/ssl_utils.py`: OpenSSL subprocess calls

### 2. Configuration Loading Pattern
**Example**: RuffConfig._load_config()
**Origin**: Python file I/O
**Consistency**: Matches pattern in src/core/

**Related Code**:
- `src/observability/`: Configuration loading
- `backend/`: Flask app configuration

### 3. Error Handling Pattern
**Pattern**: Graceful fallbacks with informative messages
**Example**: Ruff not installed → returns False with message
**Consistency**: Defensive programming throughout PyAgent

### 4. Version Comparison Pattern
**Example**: RuffVersionManager.compare_versions()
**Pattern**: Numeric comparison of version tuples
**Consistency**: Matches dependency management patterns

## External References

### Ruff Documentation
**Reference**: https://docs.astral.sh/ruff/
**Used For**:
- JSON output format understanding
- Configuration option validation
- Command-line argument specification

### Pre-Commit Framework
**Reference**: https://pre-commit.com/
**Used For**:
- Hook execution patterns
- Integration with git hooks
- Configuration format understanding

### Semantic Versioning
**Reference**: https://semver.org/
**Used For**:
- Version string parsing
- Compatibility determination

## Related Projects in Phase 1 Batch 001

### prj000111 - API Documentation
**Connection**: Both enhance code quality checks
**Shared Pattern**: Integrate with CI/CD

### prj000113 - Rust Benchmarks
**Connection**: Both manage tool versions
**Shared Pattern**: Version compatibility checking

### prj000115 - Global State
**Connection**: Both use configuration management
**Shared Pattern**: Load and validate configuration

## Quality Standards References

### Code Style
**Reference**: PyAgent .flake8 and .pylintrc
**Compliance**: ✓ Full compliance with existing standards
**Enforcement**: This project helps enforce them

### Testing Patterns
**Reference**: tests/ structure and patterns
**Compliance**: ✓ Follows existing unittest patterns

### Documentation Style
**Reference**: Existing docstrings in src/
**Compliance**: ✓ Uses same format and structure

## Version Management Strategy

### Current Ruff Version in PyAgent
**Check**: `ruff --version`
**Expected**: Latest stable (updates via dependabot/renovate)

### Version Tracking Files
1. `pyproject.toml`: Tool configuration
2. `.pre-commit-config.yaml`: Hook version
3. `requirements.txt`: Pinned version
4. GitHub Actions: Version in workflows

### Consistency Enforcement
This project ensures all three remain synchronized:
1. RuffVersionManager detects installed version
2. Compares against minimum requirement
3. Reports mismatches for resolution

## Performance Baselines

### Execution Time Budgets
**Config Load**: <10ms
**Version Check**: <500ms (includes subprocess)
**Check Single File**: ~100ms

### Scaling Characteristics
- Linear with number of files checked
- Subprocess overhead is main cost
- Caching could improve Phase 2

## Security Considerations

### No Security-Sensitive Code
- No credential handling
- No network communication
- No file modifications (read-only checks)
- Safe to run in pre-commit hooks

### Subprocess Safety
- Fixed command (no user input)
- Timeout protection (30 seconds)
- Exception handling for all subprocess calls

---
**Last Updated**: 2026-04-06
**Status**: Complete
**Integration Status**: Ready for pre-commit/CI integration
