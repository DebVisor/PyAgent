# Ql

This file describes how security scanning is configured.

## Key points
- CodeQL analysis runs via `.github/workflows/ci.yml`.
- Security policies and allowed permissions are managed in GitHub workflow YAML.

## When to update
- When workflow permissions or CodeQL queries change.
