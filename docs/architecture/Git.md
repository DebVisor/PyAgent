# Git

This document defines the repository workflow and branching conventions.

## Key points
- CI is managed by `.github/workflows/ci.yml` (unit tests, dashboard generation, CodeQL checks).
- Branching strategy and PR conventions should be documented in `CONTRIBUTING.md`.

## When to update
- When the workflow pipeline changes, or when branch naming/contribution conventions change.
